# Cookiecutter Template

`tooli init` is removed. Use a template repository:

```bash
cookiecutter gh:weisberg/tooli-template
```

Expected output:

- installable Python package
- `app.py` with a minimal Tooli app
- starter tests and CI workflow
